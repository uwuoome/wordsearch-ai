{
  "name": "literant-ai",
  "description": "AI for scrabble-like games",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest --silent=false",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy ./dist"
  },
  "devDependencies": {
    "vite": "^6.0.11",
    "vitest": "^3.0.4",
    "wrangler": "^3.106.0"
  }
}
